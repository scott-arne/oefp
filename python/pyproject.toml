[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "oefp"
version = "0.2.5"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "openeye-toolkits>=2025.2",
    "pyarrow>=24",
]

[tool.setuptools]
packages = ["oefp"]

[tool.setuptools.package-data]
oefp = ["mordred_references.json", "py.typed"]
